apiVersion: v1
kind: Service
metadata:
  name: {service_name}
  namespace: {service_namespace}
  labels:
    app: {service_name}
    analitico.ai/workspace-id: {workspace_id}
spec:
  selector:
    app: {service_name}
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
  type: NodePort
