import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { AoItemViewComponent } from './ao-item-view.component';

describe('AoItemViewComponent', () => {
  let component: AoItemViewComponent;
  let fixture: ComponentFixture<AoItemViewComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ AoItemViewComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(AoItemViewComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
