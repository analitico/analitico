import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { AoHomeViewComponent } from './ao-home-view.component';

describe('AoHomeViewComponent', () => {
  let component: AoHomeViewComponent;
  let fixture: ComponentFixture<AoHomeViewComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ AoHomeViewComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(AoHomeViewComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
