import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { AoModelListViewComponent } from './ao-model-list-view.component';

describe('AoModelListViewComponent', () => {
  let component: AoModelListViewComponent;
  let fixture: ComponentFixture<AoModelListViewComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ AoModelListViewComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(AoModelListViewComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
