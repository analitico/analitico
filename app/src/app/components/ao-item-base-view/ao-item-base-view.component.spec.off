import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { AoModelListViewComponent } from './ao-item-base-view.component';

describe('AoItemBaseViewComponent', () => {
  let component: AoItemBaseViewComponent;
  let fixture: ComponentFixture<AoModelListViewAoItemBaseViewComponentComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ AoItemBaseViewComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(AoItemBaseViewComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
